/* =====================================================================
   КОНФИГУРАЦИЯ САЙТА — единственное место, где правятся контакты/реквизиты.
   Заполни значения ниже. Поля в [квадратных скобках] — заглушки, замени их.
   ===================================================================== */

const CONFIG = {
  // --- Название магазина (выводится в шапке, hero, футере) ---
  shopName: "И.П. Жданов",
  shopTagline: "Распив нишевой парфюмерии",

  // --- Контакты для приёма заказов ---
  // Телефон в человекочитаемом виде (для отображения):
  phoneDisplay: "+7 (000) 000-00-00",
  // Телефон для ссылок tel: и WhatsApp — ТОЛЬКО цифры, с кодом страны, без + и без скобок:
  phoneRaw: "70000000000",
  // Telegram — ник без @ (например: ivanov_perfume):
  telegram: "username",
  // E-mail для отображения и mailto-фолбэка:
  email: "mail@example.com",
  // Formspree endpoint (необязательно). Если оставить пустым — письмо уйдёт через mailto.
  // Получить бесплатно на https://formspree.io → вид: "https://formspree.io/f/xxxxxxxx"
  formspreeEndpoint: "",

  // --- Какие каналы оформления показывать в корзине (true/false) ---
  channels: {
    whatsapp: true,
    telegram: true,
    email: true,
    callback: true,
  },

  // --- Реквизиты ИП (для футера, оферты, политики) ---
  legal: {
    fio: "[ИП Фамилия Имя Отчество]",
    inn: "[ИНН]",
    ogrnip: "[ОГРНИП]",
    address: "[Адрес регистрации]",
  },

  // --- Доставка/оплата (короткие тезисы, выводятся в блоке «Оплата и доставка») ---
  delivery: [
    "Отправка по России: СДЭК, Почта России, Boxberry.",
    "По городу — курьер или самовывоз по договорённости.",
    "Оплата при получении или переводом по реквизитам.",
    "Заказы собираем и отправляем в течение 1–2 рабочих дней.",
  ],

  // --- Валюта ---
  currency: "₽",

  // --- Скидка на весь каталог, % ---
  // 0 — выключить (старые цены без зачёркивания и без плашки).
  // Любое число, напр. 10 — везде покажется зачёркнутая цена и новая со скидкой.
  discountPercent: 10,
};

// делаем доступным в app.js
window.CONFIG = CONFIG;
